# What may differ between the monolith's cage and the one shed-os/cage builds.
# Nothing here is a free pass: each path was opened and read before it was
# written down, and an entry that stops matching fails the run.

# The two were not built the same way. .BUILDINFO records the monolith's cage
# built with debug and check off and this one with them on, and a debug build
# leaves different bytes in the binary. Pinned to that one pair, so the next
# time this file differs it has to be looked at again.
#
# The entry retires the next time cage is really rebuilt, since that build gets
# the same options as the monolith's.
content usr/bin/cage c2c795445793f74d9922fa9deb3c870754fcb246c2228941e20634efebd6be18..db55ddeebea0a84ecc51227c676e9e09b72231c1c5a46499a4cdbc7ff5d4910c — built with debug and check on where the monolith had them off

# scdoc writes the build date into the .TH line as it generates the page, and
# that line is the only thing that differs once the two are decompressed.
#
# Left unpinned: the date moves on every rebuild, so a pin would go stale the
# next time either side is built. The cost is that this entry forgives anything
# else that happens to the page, and the way to close it is SOURCE_DATE_EPOCH
# in the build rather than a pin here.
content usr/share/man/man1/cage.1.gz — the generated man page is stamped with the build date
