# What may differ between the monolith's shedos-migrate-to-packaged and the one
# shed-os/shedos-migrate builds. Nothing here is a free pass: each path was opened and
# read before it was written down, and an entry that stops matching fails the run.

# Reference: built from packaging/shedos-migrate-to-packaged at monolith commit 1303cdfb,
# which was origin/main at the time, under a scratch MAKEPKG_CONF carrying the same
# BUILDENV and OPTIONS the pipeline writes beside its own config. Its pkgver-pkgrel came
# from that PKGBUILD unmodified and matched the published 2026.08.09-1, so nothing was
# patched to make the two agree. Candidate: the artifact the staging database records.

# The package is arch=any and its package() installs two scripts out of tree/ — it
# compiles nothing and downloads nothing, so a second build has nothing to move. What
# differs is deliberate, and it is the pacman fence.
#
# §5.6, the single fence-writer. This tool kept its own copy of the [shedos] block and
# appended it verbatim; three copies of that text existed across the fleet and they had
# already drifted apart. It asks /usr/lib/shedos/pacman-fence for the text now, and for
# the opening marker it looks for, which is the block's own first line. The bytes it
# appends are unchanged — the suite's expected-pacman-conf fixtures are untouched and
# still match. depends gains shedos-system, which is the package that ships the library.
# Paths below are the carved repository's, pinned <monolith blob>..<carved blob>.
content tree/usr/libexec/shedman/migrate ab7ac8e18a93186e66b7b4dc9987ec47ee1470dd..6c4f0c3a75cf1ffc55d47b307e831e1582e973bf — §5.6: asks the one writer for the block and its marker instead of carrying a second copy
content PKGBUILD 928b330121ad783b78099d7eaac689d141073a41..e47fb3c21a0ffd8da1cf2d1b5a218c3c1ebf3f34 — §5.6: depends gains shedos-system, which ships the library, and the pipeline's pkgrel guard moves pkgrel past what staging carries
#
# An entry appearing here later is a real difference, not a build that drifted.
