# ShedOS Virtualization Guest-Side Helpers
# Host-side virt tools (qemu-full, libvirt, virtualbox, etc.) are
# install-on-demand. These three are kept so ShedOS feels native
# when it's run inside a VM.

qemu-guest-agent
spice-vdagent
virtualbox-guest-utils
