# ShedOS Virtualization Guest-Side Helpers
# Host-side virt tools (qemu-full, libvirt, virtualbox, etc.) are
# install-on-demand. virtualbox-guest-utils transitively pulls stock
# `linux` via VIRTUALBOX-GUEST-MODULES → virtualbox-guest-modules-arch
# (depends on linux). Removed so the install stays on shedos-kernel.

qemu-guest-agent
spice-vdagent
